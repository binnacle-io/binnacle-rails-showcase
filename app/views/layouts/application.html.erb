<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Binnacle Sample App</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= csrf_meta_tags %>
  <%= yield :binnacle_chat %>
</head>
<body role="document" class="<%= "#{params[:controller]}_#{params[:action]}" %>" data-sid="<%= session.id %>">
  <!-- Fixed navbar -->
  <nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to('Binnacle Showcase', home_path, class: 'navbar-brand') %>
      </div>
      <div id="navbar" class="navbar-collapse collapse">
        <ul class="nav navbar-nav">
          <li class="<%= 'active' if current_page?(home_path) %>"><%= link_to('Home', home_path) %></li>
          <%- if user_signed_in? %>
          <li class="<%= 'active' if current_page?(logging_path) %>"><%= link_to('Logging', logging_path) %></li>
          <li class="<%= 'active' if current_page?(exceptions_path) %>"><%= link_to('Exceptions', exceptions_path) %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Widgets <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="#">AR Cud</a></li>
              <li><a href="#"><%= link_to('Chat Widget', chat_path) %></a></li>
              <li><a href="#">Notifications</a></li>
            </ul>
          </li>
          <% end %>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <%- if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.name %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to('Sign Out', destroy_user_session_path, method: :delete) %></li>
            </ul>
          </li>
          <% else %>
          <li><%= link_to('Sign In', new_user_session_path) %></li>
          <li><%= link_to('Sign Up', new_user_registration_path) %></li>
          <% end %>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </nav>

  <div class="container" role="main">
    <%= yield %>
  </div> <!-- /container -->

  <!-- Bootstrap core JavaScript
  ================================================== -->
  <!-- Placed at the end of the document so the pages load faster -->
  <%= javascript_include_tag 'application' %>
</body>
</html>
